import React from "react";
import CartList from "../components/CartList";

const Cart = (props) => {
  return (
    <React.Fragment>
      <CartList></CartList>
    </React.Fragment>
  );
};

export default Cart;
